export const isMac = navigator.platform.toLowerCase().includes('mac')
export const isWin = navigator.platform.toLowerCase().includes('win')
export const isLinux = navigator.platform.toLowerCase().includes('linux')
