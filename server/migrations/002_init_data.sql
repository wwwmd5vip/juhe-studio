-- WARNING: This file previously contained a full MySQL dump with production data
-- including API keys, bcrypt password hashes, and channel credentials.
-- It has been replaced with a schema-only reference.
-- All table creation is handled by GORM AutoMigrate in main.go.
-- Seed data is managed by bootstrap/seed.go.
-- 
-- If you need the original dump for data recovery, see the git history.
-- This file is mounted as docker-entrypoint-initdb.d script for Docker deployments
-- but the Go server already runs AutoMigrate on startup.

-- Placeholder: schema managed by GORM AutoMigrate
SELECT 'Migration placeholder — schema managed by GORM AutoMigrate' AS info;
